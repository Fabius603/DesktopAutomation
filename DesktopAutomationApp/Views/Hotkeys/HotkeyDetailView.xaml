<UserControl x:Class="DesktopAutomationApp.Views.HotkeyDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:conv="clr-namespace:DesktopAutomationApp.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Style="{StaticResource ContentViewBase}">

    <UserControl.Resources>
        <Style x:Key="AutoOpenComboStyle" TargetType="ComboBox"
               BasedOn="{StaticResource {x:Type ComboBox}}">
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ComboBox_PreviewMouseLeftButtonDown"/>
            <EventSetter Event="GotKeyboardFocus" Handler="ComboBox_GotKeyboardFocus"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource ButtonPrimary}" Command="{Binding SaveCommand}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentSave" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Speichern" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource ButtonSecondary}" Command="{Binding CancelCommand}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Cancel" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Abbrechen" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource ButtonSecondary}" Command="{Binding BackCommand}" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ArrowLeft" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Zurueck" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitle}" Margin="0,0,12,0"/>
                <Button Style="{StaticResource ButtonSecondary}" Command="{Binding RenameCommand}" Margin="0,0,12,0" ToolTip="Umbenennen">
                    <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14"/>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- Card mit Einstellungen -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Action (Job-Auswahl) -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Action" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding ActionsView}"
                          Style="{StaticResource AutoOpenComboStyle}"
                          IsTextSearchEnabled="True"
                          DisplayMemberPath="Name"
                          TextSearch.TextPath="Name"
                          SelectedItem="{Binding SelectedJob, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          MinWidth="280"/>

                <!-- Command -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Command" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"
                          ItemsSource="{Binding CommandsView}"
                          Style="{StaticResource AutoOpenComboStyle}"
                          IsEditable="True"
                          StaysOpenOnEdit="True"
                          IsTextSearchEnabled="True"
                          SelectedItem="{Binding EditedHotkey.Action.Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          MinWidth="200"/>

                <!-- Aktiv -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Aktiv" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <CheckBox Grid.Row="2" Grid.Column="1" Margin="0,10,0,0"
                          VerticalAlignment="Center"
                          IsThreeState="False"
                          IsChecked="{Binding EditedHotkey.Active, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          ToolTip="Hotkey ist aktiv/inaktiv"/>

                <!-- Trigger -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Trigger" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBox IsReadOnly="True" Width="240"
                             Text="{Binding EditedHotkey.DisplayTrigger, Mode=OneWay}" />
                    <Button Margin="8,0,0,0"
                            Style="{StaticResource ButtonSecondary}"
                            Content="{Binding IsCapturing, Converter={StaticResource InverseBooleanToContentConverter}, ConverterParameter='Erfassen...,Erfassung läuft...'}"
                            Command="{Binding StartCaptureCommand}"
                            IsEnabled="{Binding IsCapturing, Converter={StaticResource InverseBooleanConverter}}" />
                    <Button Margin="8,0,0,0"
                            Style="{StaticResource ButtonToolbar}"
                            Content="Abbrechen"
                            Command="{Binding CancelCaptureCommand}"
                            Visibility="{Binding IsCapturing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
