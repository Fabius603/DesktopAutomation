<UserControl x:Class="DesktopAutomationApp.Views.JobStepsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DesktopAutomationApp.ViewModels"
             xmlns:jobs="clr-namespace:TaskAutomation.Jobs;assembly=TaskAutomation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:DesktopAutomationApp.Converters"
             mc:Ignorable="d"
             Style="{StaticResource ContentViewBase}">

    <UserControl.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <conv:InverseBooleanToContentConverter x:Key="InverseBooleanToContentConverter"/>

        <!-- Gemeinsame Parameterzeile -->
        <Style x:Key="ParamText" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- TemplateMatchingStep -->
        <DataTemplate DataType="{x:Type jobs:TemplateMatchingStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Links: Name -->
                <TextBlock Text="Template Matching"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>

                <!-- Abstandsspalte -->
                <Border Grid.Column="1"/>

                <!-- Rechts: Parameter untereinander -->
                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="TemplatePath: "/><Run Text="{Binding Settings.TemplatePath}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="MatchMode: "/><Run Text="Muss noch hinzugefügt werden!!"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="MultiplePoints: "/><Run Text="{Binding Settings.MultiplePoints, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="ConfidenceThreshold: "/><Run Text="{Binding Settings.ConfidenceThreshold}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="EnableROI: "/><Run Text="{Binding Settings.EnableROI, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="ROI: "/><Run Text="Muss noch hinzugefügt werden!!"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="DrawResults: "/><Run Text="{Binding Settings.DrawResults, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- DesktopDuplicationStep -->
        <DataTemplate DataType="{x:Type jobs:DesktopDuplicationStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Desktop Duplication"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>
                <Border Grid.Column="1"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="Desktop Index: "/><Run Text="{Binding Settings.DesktopIdx}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- ProcessDuplicationStep -->
        <DataTemplate DataType="{x:Type jobs:ProcessDuplicationStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Process Duplication"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>
                <Border Grid.Column="1"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="ProcessName: "/><Run Text="{Binding Settings.ProcessName}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- ShowImageStep -->
        <DataTemplate DataType="{x:Type jobs:ShowImageStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Show Image"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>
                <Border Grid.Column="1"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="WindowName: "/><Run Text="{Binding Settings.WindowName}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="ShowRawImage: "/><Run Text="{Binding Settings.ShowRawImage, Converter={StaticResource InverseBooleanToContentConverter}}" />
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="ShowProcessedImage: "/><Run Text="{Binding Settings.ShowProcessedImage, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- VideoCreationStep -->
        <DataTemplate DataType="{x:Type jobs:VideoCreationStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Video Creation"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>
                <Border Grid.Column="1"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="SavePath: "/><Run Text="{Binding Settings.SavePath}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="FileName: "/><Run Text="{Binding Settings.FileName}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="UseRawImage: "/><Run Text="{Binding Settings.UseRawImage, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="UseProcessedImage: "/><Run Text="{Binding Settings.UseProcessedImage, Converter={StaticResource InverseBooleanToContentConverter}}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- MakroExecutionStep -->
        <DataTemplate DataType="{x:Type jobs:MakroExecutionStep}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Makro Execution"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>
                <Border Grid.Column="1"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource ParamText}">
                        <Run Text="MakroName: "/><Run Text="{Binding Settings.MakroName}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="⟵ Zurück" Command="{Binding BackCommand}" Margin="0,0,12,0"/>
            <TextBlock Text="{Binding Title}" Style="{StaticResource Heading1}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8" Padding="12"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}"
                                    BorderThickness="1" CornerRadius="8">
                                <ContentPresenter Content="{Binding}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
