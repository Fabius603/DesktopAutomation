<UserControl xmlns:loc="clr-namespace:DesktopAutomationApp.Localization"
             x:Class="DesktopAutomationApp.Views.ListAutomationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:library="clr-namespace:DesktopAutomationApp.Views.Library"
             Style="{StaticResource ContentViewBase}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Style="{StaticResource ButtonPrimary}" Command="{Binding NewCommand}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Plus" Width="14" Height="14" Margin="0,0,6,0"/>
                        <TextBlock Text="{loc:Translate Key=Ui.Automation.List.NewAutomation}"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource ButtonSecondary}" Command="{Binding OpenFolderCommand}" Margin="8,0,0,0"
                        ToolTip="{loc:Translate Key=Ui.Common.OpenFolderInExplorer}">
                    <iconPacks:PackIconMaterial Kind="FolderOpen" Width="14" Height="14"/>
                </Button>
            </StackPanel>
            <TextBlock Text="{loc:Translate Key=Ui.Automation.List.Automations}" Style="{StaticResource PageTitle}"/>
        </DockPanel>
        <library:LibraryTreeView Grid.Row="1" DataContext="{Binding Library}"/>
    </Grid>
</UserControl>
