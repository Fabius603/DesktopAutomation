<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══ Shared ListBoxItem — Card-Style mit Hover/Selection ═══ -->
    <Style x:Key="StepCardListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Card"
                            CornerRadius="8"
                            Background="{DynamicResource App.Brush.SurfaceRaised}"
                            BorderBrush="{DynamicResource App.Brush.BorderSubtle}"
                            BorderThickness="1"
                            Padding="12,10"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Card" Property="BorderBrush"
                                    Value="{DynamicResource App.Brush.BorderStrong}"/>
                            <Setter TargetName="Card" Property="Background"
                                    Value="{DynamicResource App.Brush.SurfaceHover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Card" Property="Background"
                                    Value="{DynamicResource App.Brush.SelectionBackground}"/>
                            <Setter TargetName="Card" Property="BorderBrush"
                                    Value="{DynamicResource App.Brush.SelectionBorder}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══ Shared ListBox für Step-/Befehls-Listen ═══ -->
    <Style x:Key="StepCardListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource StepCardListBoxItemStyle}"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

</ResourceDictionary>
