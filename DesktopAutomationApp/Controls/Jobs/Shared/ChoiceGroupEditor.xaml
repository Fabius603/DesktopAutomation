<UserControl x:Class="DesktopAutomationApp.Controls.Jobs.Shared.ChoiceGroupEditor"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ChoiceGroupItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextSecondary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource App.Brush.SurfaceHover}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource App.Brush.BorderSubtle}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource App.Brush.SelectionBackground}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource App.Brush.SelectionBorder}"/>
                                <Setter Property="Foreground" Value="{DynamicResource App.Brush.Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource App.Brush.Surface}"
            BorderBrush="{DynamicResource App.Brush.BorderSubtle}"
            BorderThickness="1" CornerRadius="8">
        <StackPanel>
            <ListBox ItemsSource="{Binding ItemsSource, ElementName=Root}"
                     SelectedItem="{Binding SelectedItem, ElementName=Root, Mode=TwoWay}"
                     ItemTemplate="{Binding ItemTemplate, ElementName=Root}"
                     ItemContainerStyle="{StaticResource ChoiceGroupItemStyle}"
                     Background="Transparent" BorderThickness="0" Padding="2"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <Border Padding="12,10" BorderThickness="0,1,0,0"
                    BorderBrush="{DynamicResource App.Brush.BorderSubtle}"
                    Background="{DynamicResource App.Brush.SurfaceRaised}">
                <ContentPresenter Content="{Binding SelectedContent, ElementName=Root}"
                                  ContentTemplate="{Binding SelectedContentTemplate, ElementName=Root}"/>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
